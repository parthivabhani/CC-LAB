bison -d parser.y         # Generates parser.tab.c and parser.tab.h
flex lexer.l              # Generates lex.yy.c
gcc -o calc parser.tab.c lex.yy.c -lfl  # Compile with Flex library
./calc                    # Run the program

